# Dont need this file, because we're running with PulseAudio now.
#pcm.!default {
#    type plug
#    slave.pcm "dmix"
#    slave.pcm {
#        @func getenv
#        vars [ ALSAPCM ]
#        default "dmix:Intel"    # Placback to my Onboard soundcard
#        default "dmix:Headset"  # Playback to my USB headset
#    }
#}
